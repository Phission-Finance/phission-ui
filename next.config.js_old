/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  images: {
    loader: 'akamai',
    path: '',
    unoptimized: true
  }
  // basePath: '/phission-ui',
  // assetPrefix: '/phission-ui',
};

module.exports = nextConfig;
